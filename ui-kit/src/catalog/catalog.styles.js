import styled from 'styled-components';

export const StyledCatalogHeading = styled.h2`
	font-size: 36px;
	line-height: 36px;
	color: #9b5900;
	text-transform: uppercase;
	font-weight: normal;
`;

export const StyledCatalogWrapper = styled.div`
	display: flex;
	flex-direction: row;
	flex-wrap: nowrap;
	justify-content: flex-start;
`;

export const StyledCatalogContainer = styled.div`
	padding: 0 20px;
	margin-bottom: 120px;
`;


