import styled from 'styled-components';
import { pizzaLogoURL } from './images';

export const StyledHeaderLogo = styled.div`
	background-image: url(${pizzaLogoURL});
	width: 1840px;
	height: 325px;
	margin: auto;
	position: relative;
`;
